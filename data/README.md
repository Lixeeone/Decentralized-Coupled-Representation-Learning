# Local feature inputs

Place local numeric feature matrices here, or set an explicit path in a configuration. This directory's data files are ignored by Git. Do not commit pretrained weights or downloaded datasets.

See [the data contract](../docs/DATA.md). The repository does not include the original manuscript feature archive.
